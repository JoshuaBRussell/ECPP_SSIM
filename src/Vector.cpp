#include "Vector.hpp"

/*
template <unsigned int N>
Vector<N> operator*(const double& s, Vector<N> vec){
        Vector<N> result;
        
        for (unsigned int i = 0; i < N; i++){
            result[i] = s*vec[i];
        }

        return result;
}
*/
